---
name: Dreaming
about: A lesson the close of a workstream ruled for the pipeline (stage 6). Opened by the close session; applied in a pipeline session; closed by the commit that applies it.
title: "dreaming(<stage>): <the class in one line>"
labels: dreaming
---

<!--
This repo is public. The issue carries the pipeline file, the pattern
and the edit, in the pipeline's vocabulary — never a product repo, a
vendor, a person, an artifact URL, a business number, a parameter or
a key. The raw evidence lives in the workstream's private ledger,
which carries this issue's number.
-->

## Where it bites

<the path in this repo: a skill, a reference, an agent, a workflow, the blueprint's build or shell, a standard, the house rules>

## What happened

<the pattern, in one or two sentences: what the stage did, what it cost (a round, a stop, tokens, a defect that reached production), seen in `<yyyy-mm>` as entry `<id>` of that workstream's ledger>

## Why it will bite again

<one sentence: where this pattern reappears — any repo, any wave, any stage that does this>

## Suggested edit

<the line that would enter, quoted, or the mechanism in three lines — concrete enough to apply without reading anything else>

## Related

<`#n` of the same class, when the close's recurrence check found one; "none" otherwise>

## Ruling

<`issue` · `join #n` — the month, and the user's words when they carry no private detail>
