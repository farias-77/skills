# UI — <wave>

<!--
  How the UI works today and how this feature lands in it. MUST have:
  the discovery's answer up top (no UI => this file says so and stops);
  the current patterns the feature must grow (from the front code and
  the Claude Design library); every screen with the components that
  carry it and EVERY state the stories imply (the bad-paths tables are
  the checklist — a screen with only its happy state is half a screen);
  the screen index into ui-screens/; and the Claude Design project link
  with the published card list — publishing there is the standard of
  work, the user validates the visuals in the project.
-->

Discovery's answer: <no UI · UI exists · we build it>

## The patterns this feature grows

| Pattern | Where it lives today | How this feature uses it |
|---|---|---|
| <component/token/convention> | <file / library card> | <use> |

## Screens

### <screen name> (covers S-00N)

- **Carried by:** <existing components>
- **New here:** <new component + the decision block that justifies it, or "nothing">
- **States:** loading · empty · error · permission-denied · <the story's bad-path outcomes, each with its treatment>
- **Source:** `ui-screens/<file>.html`

## Published to Claude Design

- **Project:** <link — where the user validates>
- **Cards:** <group / names published this wave>

## References

- <research file · URL · internal code path · library cards read>
