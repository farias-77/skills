# codex/

Placeholder. The pipeline runs in Claude Code (`claude/`); nothing is
built for Codex today. If a Codex chair returns, its skills, agents and
project config live here, next to the shared `docs/`.
