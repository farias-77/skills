# Dreaming — <workstream> — the board

<!--
Every friction the harvest brought is an entry: what happened, where
it bit, the evidence, the session's suggestion (class, destination,
the edit), the recurrence verdict, and — after the stop — the ruling
and the issue number. The user rules by reading: the suggestion is
the default, he vetoes or changes in prose, "apply" rules the rest.
Nothing is opened, discarded or parked without his ruling. Private
file: the raw evidence lives here; the issues carry none of it.
-->

**Sources read:** `harvest/documents.json` (<n> frictions),
`harvest/execution.json` (<n>), `harvest/release.json` (<n>),
`harvest/notes.json` (<n>, <k> `[user]`, <t> taste),
`harvest/previous.json` (<p> parked from `<previous slug>`).

**Recurrence:** <n> pipeline candidates checked against
`<owner>/<pipeline repo>`: <a> `new` · <b> `open #n` · <c> `closed #n`
(<d> with the rule still in the text — questions).

**Precision per lens this workstream** (the calibration ruler): <stage
· lens · found · sustained · dismissed>, the off ones named.

Ranked by what each entry would have saved this demand. The `[user]`
entries come first.

---

## A · `[user]` entries — confirm the edit and move on

### U-1 · <title>

- **What happened:** <the pattern; where it bit: `file:line`, "the quote">.
- **Class:** pipeline · **Destination:** `<path in the pipeline repo>`.
- **The edit:** <the line that would enter, or the mechanism in three lines>.
- **Recurrence:** `new` · `open #n` · `closed #n` (rule <in|not in> the text).
- **Default:** issue.
- **Ruling:** — → `issue #n` · `join #n` · `discard` · `park` · "<his words>"

## B · Pipeline candidates — ranked

### P-1 · <title>

- **What happened:** …
- **Would have saved:** <rounds, tokens, hours, a stop, a defect>.
- **Class:** pipeline · **Destination:** `<path>`.
- **The edit:** …
- **Recurrence:** …
- **Default:** issue.
- **Ruling:** —

## C · Taste — asked, never defaulted

### T-1 · <the pattern, from taste-notes.md>

- **Instances:** <the rulings behind it, ids and dates>.
- **What it could become:** a standard line (`docs/standards/<file>`) · a skill or agent line (`<path>`) · stays a taste note.
- **Ruling:** — (question at the stop)

## D · Venture and repo — pendencies with an owner, never a public issue

### V-1 · <title>

- **What happened:** …
- **Class:** venture · **Destination:** `<the venture's own docs>` · **Owner:** <who>.
- **Ruling:** —

### R-1 · <title>

- **Class:** repo · **Destination:** `<repo>/docs/<file>` · **Owner:** <who>.
- **Ruling:** —

## E · Suggested discards — incident, not class (rescuable)

- **X-1** <what happened> — <one line of why it does not generalize>. **Ruling:** —

## Parked from `<previous slug>`

### <original id> · <title> (parked <date>)

- **Then:** <its suggestion then>. **Now:** <recurrence verdict>. **Section:** <A–E>.
- **Ruling:** —

---

## Rulings

<!-- written at the stop, one per entry, his words verbatim; then the
issue numbers after step 5 -->

| Entry | Session | Ruled | Issue | His words |
|---|---|---|---|---|
| U-1 | issue | issue | #41 | — |
| P-3 | issue | issue | #44 | "the window must contain the event by construction" |
| T-2 | question | discard | — | "it is taste, leave it in the notes" |
| X-5 | discard | park | — | "I want to see this one again" |

Issues opened: <n> · joined: <n> · discarded: <n> · parked: <n>.
