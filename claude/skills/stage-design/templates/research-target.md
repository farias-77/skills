# <target> — researched <date>

<!--
  One file per deep-research workflow, one workflow per target — never a
  global sweep. MUST have: the scope line, every finding labeled, sources
  per finding (URL for external; command + output for internal), and the
  NOT-confirmed section — absence of evidence is a finding too.
  Labels never get promoted downstream: what is [inference] here cannot
  appear as fact in a design document.
-->

Scope: <what this wave needs from this target — one line>

## Findings

- [fact] <claim> — <source URL, or the CLI command + its output for internal targets>
- [inference] <claim> — inferred from <what>
- [heuristic] <claim> — practice, not guarantee

## What was NOT confirmed

- <what we looked for and could not establish — as important as the findings>
